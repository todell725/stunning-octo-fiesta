# Copy this to .env and edit before running docker compose

# Port the app is available on (default: http://localhost:8080)
APP_PORT=8080

# OCR provider: tesseract (default) | google-vision | aws-textract
OCR_PROVIDER=tesseract
